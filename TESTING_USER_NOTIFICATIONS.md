# Инструкция по тестированию уведомлений об изменениях пользователей

## Что было реализовано:

1. **Автоматическое отслеживание изменений**: Когда пользователь меняет имя, фамилию или username, это записывается в таблицу `telegram_user_history`

2. **Отправка уведомлений**: Бот отправляет уведомление об изменениях в группы, где:
   - Бот является администратором
   - Пользователь состоит в группе (статус 'active')
   - Функция уведомлений включена (по умолчанию включена)

3. **Управление через бот**: Администратор может включать/выключать уведомления через меню бота

## Как проверить работу:

### Шаг 1: Убедиться, что настройки включены

1. Откройте бот в Telegram
2. Нажмите "Управление чатами"
3. Выберите вашу группу
4. Нажмите "🔔 Уведомления об изменениях пользователей"
5. Убедитесь, что уведомления включены (по умолчанию включены)

### Шаг 2: Проверить, что пользователь состоит в группе

Проверьте в базе данных:

```sql
SELECT * FROM chat_members 
WHERE telegram_user_id = <USER_ID> 
AND status = 'active';
```

### Шаг 3: Вызвать изменение данных пользователя

Есть 3 способа:

#### Способ 1: Естественный (рекомендуется)
1. Попросите пользователя изменить свое имя/фамилию/username в настройках Telegram
2. Попросите пользователя написать сообщение в группу
3. Бот автоматически обнаружит изменения и отправит уведомление

#### Способ 2: Через админ панель
1. Откройте админ панель на странице "Проверка пользователей"
2. Запустите автоматическую или ручную проверку пользователей
3. Бот обновит данные всех пользователей и отправит уведомления об изменениях

#### Способ 3: Тестовый скрипт
```bash
cd /Users/s3s3s/Desktop/final\ word\ 2.8.1/backend
source venv/bin/activate
python test_user_change_notification.py
```
Введите Telegram User ID и скрипт симулирует изменение

### Шаг 4: Проверить логи

После изменения проверьте логи сервера Python:

Вы должны увидеть сообщения:
```
[USER_CHANGE] User <ID>: first_name changed from '<old>' to '<new>'
[USER_CHANGE] Bot instance available, sending notifications...
[NOTIFY] Starting notification process for user <ID>, field first_name
[NOTIFY] User found: <name>
[NOTIFY] Found X chats where user is active member and notifications enabled
[NOTIFY] Sending to chat <CHAT_ID> (<CHAT_TITLE>)
[NOTIFY] Successfully sent to chat <CHAT_ID>
```

### Шаг 5: Проверить уведомление в группе

В группе должно появиться сообщение:

```
🔔 User Profile Changed

User: <ссылка на пользователя>
First Name:
  Was: <старое значение>
  Now: <новое значение>
```

## Возможные проблемы:

### Уведомления не приходят

1. **Bot instance not available**: Проверьте, что в обработчиках сообщений передается параметр `bot`
2. **No chats found**: Проверьте, что:
   - Пользователь добавлен в `chat_members` с status='active'
   - Бот добавлен в группу как администратор
   - `notify_on_user_changes = True` для чата
3. **Permission denied**: Бот должен иметь право отправлять сообщения в группу

### Проверка в базе данных:

```sql
-- Проверить настройки чата
SELECT id, title, notify_on_user_changes, is_active 
FROM chats 
WHERE id = <CHAT_ID>;

-- Проверить членство пользователя
SELECT * FROM chat_members 
WHERE chat_id = <CHAT_ID> 
AND telegram_user_id = <USER_ID>;

-- Проверить историю изменений
SELECT * FROM telegram_user_history 
WHERE telegram_user_id = <USER_ID> 
ORDER BY changed_at DESC 
LIMIT 10;
```

## Отключение уведомлений:

1. В боте: "Управление чатами" → Выбрать группу → "🔔 Уведомления" → "❌ Выключить"
2. Через API: PATCH `/api/chats/{chat_id}` с `{"notify_on_user_changes": false}`
