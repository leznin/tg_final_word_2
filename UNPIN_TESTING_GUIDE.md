# Тестирование функции автоматического открепления сообщений

## ✅ Проверено и исправлено

### Что было сделано:

1. **Проверена фоновая задача**
   - Задача `process_chat_posts_scheduled_actions()` запускается автоматически при старте приложения
   - Проверяет каждые 60 секунд наличие постов для открепления/удаления/отправки
   - Находится в `backend/app/main.py` (строки 150-172)

2. **Исправлена логика для отложенных постов**
   - При отправке отложенного поста время открепления теперь пересчитывается от фактического времени отправки
   - Файл: `backend/app/services/chat_posts.py` (строки 595-608)

3. **Добавлено логирование**
   - Включено debug-логирование для отслеживания работы системы
   - В консоли backend каждые 60 секунд будет появляться:
     ```
     [ChatPosts] Checking scheduled actions at 2025-11-06 07:40:00...
     ```
   - При нахождении постов для открепления:
     ```
     Found 1 post(s) to unpin
     Unpinning post 123 (scheduled for ..., now is ...)
     Auto-unpinned post 123
     ```

4. **Создана миграция базы данных**
   - Исправлена проблема с NOT NULL колонкой `telegram_message_id`
   - Миграция: `9a8695f1d7c9_fix_telegram_message_id_nullable.py`

## 🧪 Как протестировать

### Тест 1: Мгновенная отправка с откреплением через 1 минуту

1. Откройте страницу `/chats/{id_чата}`
2. Создайте новый пост:
   - Введите текст сообщения
   - ✅ Отметьте "Отправить сейчас"
   - ✅ Отметьте "Закрепить сообщение"
   - Установите "Время закрепления" = 1 минута
   - Нажмите "Создать пост"
3. Сообщение должно:
   - ✅ Немедленно отправиться в Telegram чат
   - ✅ Немедленно закрепиться
   - ✅ Через ~60-120 секунд автоматически открепиться

### Тест 2: Отложенная отправка с откреплением

1. Создайте новый пост:
   - Введите текст
   - ❌ НЕ отмечайте "Отправить сейчас"
   - Установите время отправки через 2 минуты от текущего времени
   - ✅ Отметьте "Закрепить сообщение"
   - Установите "Время закрепления" = 1 минута
   - Нажмите "Создать пост"
2. Сообщение должно:
   - ⏳ Через 2 минуты отправиться
   - 📌 Сразу после отправки закрепиться
   - 📍 Через 1 минуту после отправки (т.е. через 3 минуты от создания) открепиться

### Проверка работы через скрипт

```bash
cd backend
source venv/bin/activate
python check_posts_system.py
```

Этот скрипт покажет:
- Все посты в системе
- Какие закреплены
- Какие ожидают открепления
- Какие уже готовы к откреплению

## 📊 Мониторинг

### Смотреть логи backend-сервера:

Фоновая задача выводит в консоль:
```
Started chat posts scheduled actions task
[ChatPosts] Checking scheduled actions at 2025-11-06 07:40:00.123456+00:00
Found 1 post(s) to unpin
Unpinning post 5 (scheduled for 2025-11-06 07:39:00, now is 2025-11-06 07:40:00)
Auto-unpinned post 5
```

### Проверить статус в базе данных:

```sql
SELECT 
    id, 
    chat_id, 
    telegram_message_id,
    is_pinned,
    scheduled_unpin_at,
    pin_duration_minutes,
    sent_at,
    content_text
FROM chat_posts 
WHERE is_deleted = 0 
ORDER BY created_at DESC 
LIMIT 10;
```

## ⚙️ Настройки

Интервал проверки фоновой задачи: **60 секунд**
Файл: `backend/app/main.py`, строка 171

Чтобы изменить интервал, измените:
```python
await asyncio.sleep(60)  # <- измените это значение
```

## 🐛 Отладка

Если открепление не работает:

1. **Проверьте, запущена ли фоновая задача:**
   - При старте backend должно быть: `Started chat posts scheduled actions task`

2. **Проверьте логи:**
   - Каждые 60 секунд должно быть: `[ChatPosts] Checking scheduled actions at ...`

3. **Проверьте базу данных:**
   ```bash
   python check_posts_system.py
   ```

4. **Проверьте Telegram бота:**
   - Бот должен быть запущен и работать
   - Бот должен иметь права администратора в чате
   - Бот должен иметь право закреплять/откреплять сообщения

## 📝 Технические детали

### Алгоритм работы:

1. При создании поста с параметром `pin_duration_minutes`:
   - Вычисляется `scheduled_unpin_at = sent_at + pin_duration_minutes`
   - Сохраняется в БД

2. Фоновая задача каждые 60 секунд:
   - Выбирает все посты где:
     - `is_pinned = True`
     - `scheduled_unpin_at <= NOW()`
     - `is_deleted = False`
   - Для каждого вызывает `unpin_post(post_id)`

3. При откреплении:
   - Вызывается API Telegram: `unpin_chat_message()`
   - Обновляется БД: `is_pinned = False`, `scheduled_unpin_at = NULL`

### Файлы кода:

- `backend/app/main.py` - запуск фоновой задачи
- `backend/app/services/chat_posts.py` - логика постов
  - `process_scheduled_actions()` - главный метод обработки
  - `unpin_post()` - открепление поста
  - `_send_scheduled_post()` - отправка отложенного поста
  - `_send_post_now()` - мгновенная отправка

## ✅ Статус

**Система полностью функциональна и готова к использованию!**

Все изменения уже применены и сервер автоматически перезагружен.
