# История изменений пользователей в Mini App

## Обзор изменений

Добавлена функция отображения истории изменений пользователей из таблицы `telegram_user_history` на странице поиска пользователей в Mini App.

## Изменённые файлы

### Backend

1. **`backend/app/schemas/mini_app.py`**
   - Добавлена новая схема `UserHistoryEntry` для представления записи истории
   - Обновлена схема `UserSearchResult`:
     - Изменено поле `id` на `telegram_user_id`
     - Добавлено поле `account_creation_date`
     - Добавлено поле `history: List[UserHistoryEntry]`

2. **`backend/app/services/mini_app.py`**
   - Импортирована модель `TelegramUserHistory`
   - Обновлён метод `search_users()`:
     - Теперь ищет пользователей в таблице `telegram_users` вместо `users`
     - Для каждого найденного пользователя загружает историю изменений из `telegram_user_history`
     - История сортируется по времени изменения (новые записи первыми)

### Frontend

1. **`frontend/src/types/mini-app.ts`**
   - Добавлен интерфейс `UserHistoryEntry` с полями:
     - `id`: идентификатор записи
     - `field_name`: название изменённого поля
     - `old_value`: старое значение
     - `new_value`: новое значение
     - `changed_at`: время изменения
   - Обновлён интерфейс `UserSearchResult`:
     - Изменено `id` на `telegram_user_id`
     - Добавлено `account_creation_date`
     - Добавлено `history: UserHistoryEntry[]`

2. **`frontend/src/pages/MiniAppUserSearch.tsx`**
   - Добавлены вспомогательные функции:
     - `formatDate()`: форматирование даты в локальном формате
     - `getFieldNameDisplay()`: перевод названий полей на русский
   - Обновлён компонент отображения результатов поиска:
     - Добавлено отображение даты создания аккаунта
     - Добавлена секция "История изменений" для каждого пользователя
     - История отображается в виде списка с:
       - Названием изменённого поля (на русском)
       - Датой и временем изменения
       - Старым и новым значением со стрелкой перехода
     - Секция истории скрывается, если нет записей
     - Добавлена прокрутка для длинных списков истории (max-height: 240px)

## Функциональность

### Поиск пользователей

Поиск работает **только** по следующим критериям:
- **ID пользователя**: введите числовой Telegram ID для точного поиска
- **Username**: введите username с @ или без (например, `@username` или `username`)

⚠️ **Важно**: Поиск по имени и фамилии отключён для точности результатов.

Поиск нечувствителен к регистру и поддерживает частичное совпадение для username (для ID - только точное совпадение).

### Обнаружение похожих аккаунтов

Система автоматически группирует пользователей с **одинаковой датой создания аккаунта**:
- Если несколько аккаунтов созданы в одну и ту же секунду - это подозрительно и указывает на связанные аккаунты
- **Первый аккаунт** в группе отображается полностью
- **Остальные похожие аккаунты** отображаются с замаскированными данными:
  - Имя и фамилия частично скрыты звёздочками (например: `Ив****`, `Пе****`)
  - ID и username остаются полными для идентификации
  - Добавляется бейдж **"🔄 Похожий"** оранжево-красного цвета

Это помогает легко идентифицировать потенциально связанные или автоматически созданные аккаунты.

### Backend API

Endpoint `/api/mini-app/search-users` теперь возвращает:
```json
{
  "results": [
    {
      "telegram_user_id": 123456789,
      "username": "example_user",
      "first_name": "John",
      "last_name": "Doe",
      "language_code": "en",
      "is_premium": false,
      "is_bot": false,
      "account_creation_date": "2024-01-15T10:30:00Z",
      "created_at": "2024-01-15T10:30:00Z",
      "updated_at": "2024-11-08T12:45:00Z",
      "history": [
        {
          "id": 1,
          "field_name": "username",
          "old_value": "old_username",
          "new_value": "example_user",
          "changed_at": "2024-11-08T12:45:00Z"
        }
      ]
    }
  ],
  "total": 1,
  "limit": 20,
  "offset": 0
}
```

### Frontend UI

Для каждого найденного пользователя отображается:
- Основная информация (имя, username, ID)
- Дата создания аккаунта (если доступна)
- Бейджи:
  - **🔄 Похожий** (оранжево-красный) - для аккаунтов с замаскированными данными
  - **⭐ Premium** (жёлто-оранжевый) - для Premium пользователей
  - **🤖 Bot** (серый) - для ботов
- **Раздел "История изменений"** (если есть записи):
  - Количество изменений
  - Список всех изменений с:
    - Названием поля (Имя, Фамилия, Username)
    - Временем изменения
    - Старым значением → Новым значением

### Маскировка похожих аккаунтов

При обнаружении нескольких пользователей с **одинаковой датой создания аккаунта**:
1. Первый аккаунт отображается полностью
2. Остальные аккаунты показываются с маской:
   - Первые 2 символа имени/фамилии + звёздочки
   - Пример: `Иван` → `Ив**`, `Петров` → `Пе*****`
3. ID и username остаются полными
4. Добавляется визуальный индикатор "🔄 Похожий"

**Почему по дате создания?**
Если несколько аккаунтов созданы в одну и ту же секунду, это явный признак автоматического создания или связанных аккаунтов.

## Таблицы базы данных

### `telegram_users`
Хранит актуальные данные о пользователях Telegram.

### `telegram_user_history`
Хранит историю изменений полей пользователей:
- `telegram_user_id`: ID пользователя
- `field_name`: название изменённого поля ('first_name', 'last_name', 'username')
- `old_value`: предыдущее значение
- `new_value`: новое значение
- `changed_at`: время изменения

## Преимущества

1. **Полная история изменений**: можно отследить все изменения имени, фамилии и username пользователя
2. **Удобный интерфейс**: история отображается прямо в карточке пользователя
3. **Локализация**: названия полей на русском языке
4. **Производительность**: история загружается только для найденных пользователей
5. **UX**: прокрутка для длинных списков истории, визуальное разделение записей

## Использование

1. Откройте Mini App в Telegram
2. Введите запрос для поиска пользователя
3. В результатах поиска вы увидите найденных пользователей
4. Для каждого пользователя будет отображена актуальная информация и история изменений (если есть)

## Примечания

- История отображается в обратном хронологическом порядке (новые записи первыми)
- Если у пользователя нет истории изменений, раздел "История изменений" не отображается
- Максимальная высота списка истории - 240px с прокруткой
- Пустые значения отображаются как "(пусто)"
